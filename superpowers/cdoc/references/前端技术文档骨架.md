# 前端业务技术开发文档骨架

这是一份给 AI 直接套用的章节骨架。

使用规则：

1. 直接按本骨架输出，不要保留无关示例值。
2. 没有信息的地方写“待确认”或“需补充”，不要编造。
3. 如果当前需求不涉及某一节，可以删掉该节，不要保留空标题。
4. 如果用户提供了团队格式要求，直接在当前文档里对齐样式和章节命名，不要引入旧示例模板。

---

# {{需求名称}} 前端技术方案

| 字段 | 内容 |
| --- | --- |
| 文档编号 | {{可选}} |
| 需求名称 | {{需求名称}} |
| 需求 ID | {{可选}} |
| 前端 Owner | {{可选}} |
| 文档状态 | 草稿 / 评审中 / 已确认 |
| 最后更新时间 | {{日期}} |

## 1. 需求概述

### 1.1 背景

- 说明业务背景。
- 说明为什么要做这次需求。

### 1.2 目标

- 说明本次前端方案要解决的核心问题。
- 如有明确指标或体验目标，写清楚。

### 1.3 范围

#### 本次范围

- 页面：{{页面 / 模块列表}}
- 终端：{{H5 / PC / 小程序 / 管理台 / 其他}}
- 功能：{{本次覆盖的能力}}

#### 非本次范围

- {{明确排除的内容；如果暂无则写“待确认”}}

### 1.4 需求拆分

- 按页面、流程步骤或业务模块拆分需求点。
- 不要只列标题，要体现前端需要承接的交互或状态变化。

## 2. 业务流程与交互

### 2.1 主流程

- 用文字说明主链路。
- 写清楚起点、关键动作、结果状态。

### 2.2 分支与异常流程

- 说明条件分支、权限差异、失败态、回退路径。

### 2.3 页面状态

- 加载态
- 空态
- 错误态
- 成功态
- 可编辑 / 不可编辑态

## 3. 技术实现

### 3.1 模块与目录落点

- 说明代码会落在哪些目录、页面、组件、store、hooks 或 utils 下。

### 3.2 任务拆分

按可执行粒度拆分，例如：

- 页面容器层
- 业务组件层
- 状态管理层
- 接口接入层
- 埋点 / 权限 / 容错处理

### 3.3 组件拆分

对每个核心组件说明：

#### {{组件名}}

- 组件职责：{{负责什么}}
- 输入：{{props / store / route / context}}
- 输出：{{事件 / 回调 / 状态更新}}
- 通信方式：{{props / event bus / store / context / URL 参数}}
- 复用性：{{是否可复用，复用范围}}

### 3.4 公共能力

- 公共 hooks
- 公共 utils
- 公共常量 / 枚举
- 权限控制
- 埋点
- 缓存 / 草稿 / 本地存储

## 4. 数据与接口

### 4.1 数据流

- 数据来源：{{接口 / 本地缓存 / URL 参数 / 外部容器}}
- 数据转换：{{字段映射、格式转换、组合逻辑}}
- 数据持有者：{{页面状态 / store / 表单实例 / 缓存}}

### 4.2 接口清单

对每个关键接口说明：

#### {{接口名称}}

- 用途：{{做什么}}
- 请求时机：{{页面进入 / 用户点击 / 提交前校验 / 懒加载}}
- 关键参数：{{如未知写待确认}}
- 返回字段：{{如未知写待确认}}
- 错误处理：{{toast / 重试 / 降级 / 回退}}

### 4.3 状态管理

- 哪些状态放本地。
- 哪些状态放全局。
- 哪些状态需要跨页面或跨端共享。

## 5. 非功能性需求

### 5.1 性能

- 首屏性能
- 请求频次控制
- 大列表 / 大表单优化
- 缓存与懒加载策略

### 5.2 兼容性

- 浏览器 / 设备 / 小程序 / PC 差异
- 容器能力依赖

### 5.3 安全性

- 鉴权与权限校验
- 敏感信息展示边界
- 前端校验与服务端校验边界

### 5.4 用户体验

- 反馈机制
- 容错提示
- 可恢复性
- 可理解性

## 6. 联调与验证关注点

- 需要与后端确认的字段或枚举
- 需要与产品确认的交互边界
- 需要联调验证的异常场景
- 需要补充测试或埋点验证的部分

## 7. 风险评估与应对

| 风险 | 可能性 | 影响 | 应对措施 |
| --- | --- | --- | --- |
| {{风险点}} | 高 / 中 / 低 | 高 / 中 / 低 | {{可执行措施}} |

## 8. 待确认项

- {{待确认项 1}}
- {{待确认项 2}}
- {{待确认项 3}}